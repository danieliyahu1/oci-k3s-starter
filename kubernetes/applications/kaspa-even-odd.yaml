# Kaspa Even Odd owns its Kubernetes manifests in its deploy/ directory.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kaspa-even-odd
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/danieliyahu1/kaspa-even-odd.git
    targetRevision: HEAD
    path: deploy
  destination:
    server: https://kubernetes.default.svc
    namespace: kaspa-even-odd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

# ── Terraform / OpenTofu ────────────────────────────────────────────────────────
.terraform/
.terraform.lock.hcl.bak
*.tfstate
*.tfstate.*
crash.log

# Your real values. terraform.tfvars.example is committed; this is not.
terraform.tfvars
*.auto.tfvars

# Saved plans. A plan is a point-in-time artefact of one machine's state and can
# contain resolved values — it has no business in git.
*.tfplan

# Backend config, if you use a remote backend (it holds bucket + credentials).
backend.hcl

# Created by scripts/enable-state-encryption.sh. It must NOT be committed: a clone that
# gets the encryption block with no passphrase crashes OpenTofu. Enable it per-machine.
state-encryption.tf

# ── Secrets — never commit ─────────────────────────────────────────────────────
*.pem
*.key
!*.pub
.env
.env.*
kubeconfig
kubeconfig.*
*.kubeconfig

# The box's SSH endpoint, generated by `tofu apply` for the local scripts.
# See terraform/ssh-endpoint.tf and scripts/ssh-endpoint.ps1 / .sh.
.ssh-endpoint

# ── OS / editor ────────────────────────────────────────────────────────────────
.DS_Store

# ── Local agent tooling ────────────────────────────────────────────────────────
.opencode/
opencode.json
.playwright/
